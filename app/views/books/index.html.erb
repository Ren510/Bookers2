  <% if @book.errors.any? %>
    <div class = "alert">
      <div class= "alert-box">
      <ul>
        <h3>2 errors prohibited this book from being saved:</h3>
        <% @book.errors.full_messages.each do |message| %>
        <div class="error_explanation"></div>
        <%= message %>
        <% end %>
  <% end %>
    <%= render 'users/user', user: @user,book: @book %>

     <div class="col-md-8 offset-md-1">
      <h2>Books</h2>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th>Title</th>
            <th>Opinion</th>
          </tr>
            <% @books.each do |book|%>
          <tr>
            <td><%= link_to user_path(book.user_id) do %>
            <%= attachment_image_tag book.user, :profile_image, :fill, 30, 30, format: 'jpeg',fallback: 'no_image-ede28af4bad96368ef41be1b4be1cffce0d9402540f75d17895e32cb0d2af5e6',size: '30x30' %>
            <% end %>
            </td>
            <td> <%= book.title %> </td>
            <td> <%= book.body %> </td>
          </tr>
            <% end %>
            <th colspan="3"></th>
        </thead>
        <tbody>
        </tbody>
      </table>
    </div>
  </div>
</div>
   <div class="container mt-5">
    <div class="row">
    	<div class="mx-auto">
    		<p>CopyRight Infratop.inc</p>
    	</div>
    </div>
  </div>