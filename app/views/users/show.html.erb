<%= render 'users/user', user: @user,book: @book %>

 <div class="col-md-8 offset-md-1">
      <h2>Users</h2>
      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th>Image</th>
            <th>name</th>
          </tr>
            <% @users.each do |user| %>
          <tr>
            <td> <%= attachment_image_tag @user, :profile_image, :fill, 30, 30, format: 'jpeg' %> </td>
            <td> <%= user.name %> </td>
            
          </tr>
            <% end %>
            <th colspan="3"></th>
        </thead>
        <tbody>
        </tbody>
      </table>
    </div>
  </div>
</div>
   <div class="container mt-5">
    <div class="row">
    	<div class="mx-auto">
    		<p>CopyRight Infratop.inc</p>
    	</div>
    </div>
  </div>