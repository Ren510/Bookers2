  <div class="favorite-button">
    <% if user_signed_in? %>
    <% if book.favorited_by?(current_user) %>
     <%=link_to "💔 #{book.favorites.count}",destroy_favorite_path(book.id), method: :delete, class: 'btn btn-default', remote: true %>　
      <% else %>
     <%= link_to "💖 #{book.favorites.count}", create_favorite_path(book.id), method: :post, class: 'btn btn-default', remote: true %>   
    <% end %>
    <% end %>
  </div>